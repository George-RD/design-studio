# Issue tracker: GitLab

Issues and specs for this repo live as GitLab issues. Use `glab` for issue operations. Merge requests are not a triage request surface unless the repository config explicitly enables them. Prefer native blocking links where available; otherwise record `Blocked by` references in issue descriptions.
