# Domain Docs

Read `CONTEXT.md` or `CONTEXT-MAP.md` when present and relevant ADRs before proposing architecture work. Use the glossary's vocabulary in tickets and code. Surface ADR conflicts explicitly rather than silently overriding them. Default to a single-context layout unless the repo is genuinely multi-package.
