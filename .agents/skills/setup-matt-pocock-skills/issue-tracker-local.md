# Issue tracker: Local Markdown

Issues and specs live under `.scratch/<feature-slug>/`. The spec is `spec.md`; implementation tickets are one file per ticket under `issues/`, numbered in dependency order. Record triage state and blockers in each ticket file.
